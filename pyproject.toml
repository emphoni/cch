[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "cch"
version = "0.1.0"
description = "Claude Code Helper - save and manage session contexts"
requires-python = ">=3.9"

[project.scripts]
cch = "cch:main"
